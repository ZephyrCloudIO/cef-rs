# Changelog

## Unreleased

- build(cef): align the extension bindings with Tauri's 151.8.1 requirement.
  Keep native SDK `151.1.0-HEAD.3586` in the package build metadata.
  Keep the generated bindings, extension APIs, and Windows wrapper support.
